---
interface Props {
  text: string;
  className?: string;
}

const { text, className } = Astro.props;
---

<div class="sectionTitle">
  <div class="l-container">
    <h2 lang="en" class:list={['sectionTitle__text', className]}>{text}</h2>
  </div>
</div>

<style lang="scss">
  @use '../assets/scss/global' as *;

  .sectionTitle {
    overflow: hidden;
  }

  .sectionTitle__text {
    display: flex;
    align-items: center;
    column-gap: rem(16);
    font-size: fluid(44, 88, var(--sp-comp-width), 1920);
    line-height: 1.2;

    @include mq(md) {
      column-gap: rem(32);
    }

    &::after {
      content: '';
      display: inline-block;
      width: 100vw;
      height: 1px;

      // margin-right: calc(50% - 50vw);
      margin-inline-end: calc(50% - 50vw);
      overflow: hidden;
      background-color: var(--main-color);
    }

    &.-reverse {
      flex-direction: row-reverse;

      &::after {
        margin-right: 0;
        margin-left: calc(50% - 50vw);
      }
    }
  }
</style>
